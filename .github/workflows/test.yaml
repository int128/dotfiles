name: test

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get install -y zsh
      - run: ./install
      - run: zsh -l -c env

      - run: ln -sf .bashrc ~/.bashrc
      - run: ln -sf .Xdefaults ~/.Xdefaults
      - run: sudo apt-get install -y xvfb xterm
      - run: xvfb-run --auto-servernum bash .github/screenshot.sh
      - uses: actions/upload-artifact@v3
        with:
          name: screenshot
          path: screenshot.jpg
